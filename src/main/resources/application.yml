spring:
  application:
    name: ${APP_NAME:happy-java-api}
  servlet:
    multipart:
      file-size-threshold: 1MB
      max-file-size: 500KB
      max-request-size: 1MB
auth:
  api:
    header-name: X-API-KEY
    secret: ${API_KEY_SECRET}
